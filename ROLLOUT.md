# GIGA Standard v4 ロールアウト進捗

| リポジトリ | 型 | P0 | P1(表示/PWA) | P2 | P3 | ゲート | 備考 |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| online-manuscript-paper-pro | A | ✅ | ✅ | ✅ | ✅ | ✅ | 第3群。manifest の id 衝突を解消。§H1/H2 は別案件 |

## このリポジトリで分かったこと（他リポジトリへ展開するときの注意）

1. **`"id": "./"` は「相対パスだから安全」ではない。**
   manifest の `id` はマニフェストの置き場所ではなく**オリジンを基準に**解決される。
   `gigayama.github.io` 配下のアプリが揃って `"./"` と書いていると、
   全部が `https://gigayama.github.io/` という同じ識別子になる。
   **他リポジトリでも真っ先に確認すべきはここ。**

2. **`user-scalable=no` は「児童画面だから付ける」ではない。**
   文章を読む・書くアプリでは拡大できないことのほうが害が大きい。
   ドリル系（一問一答で誤ズームが学習を止める）とは分けて判断する。

3. **タップ44pxは `@media (pointer: coarse)` で切り分けると事故らない。**
   全端末に一律で適用すると、先生用の密なツールバーが間延びする。
   実測では指操作の端末（320/375/810px）で44px未満が0個になり、
   マウス端末のレイアウトは変わらなかった。

4. **アイコンのパレット化は方式で桁が変わる。**
   PIL の MEDIANCUT では 211KB→133KB にしかならなかったが、
   FASTOCTREE では同じ誤差水準で 211KB→21.8KB になった。
   ディザリングが入ると平坦な面が壊れ、PNG が縮まなくなるため。

5. **品質ゲートの文字列一致は、コメントを除いてから判定する。**
   「localStorage には触れない」という注意書きが違反として検出されると、
   正しい注意書きを消す方向に力が働いてしまう。

## 未決（人間が決めること）

- `online-manuscript-paper` / `-lite` / `-pro` / `Online-Publisher-pro` の4系統の整理方針
- ブラウザ内 Babel 変換をやめるか（AUDIT.md §H1）。やめれば CSP も書ける（§H2）
